#ifndef _LOOKUP
#define _LOOKUP

#include <stdlib>
#include <stdio>
#include <string.h>
#include "med.h"
#include "trie.h"

extern int valid(char *);
extern bool check(char *, char *);
extern void travel(char *,int);

#endif